# Netlify's dev function bundler (zip-it-and-ship-it) misses libsql's
# platform-specific optional dependencies when they live only inside pnpm's
# virtual store. Hoisting them to the root node_modules makes them resolvable
# in local function serving and bundling.
public-hoist-pattern[]=*libsql*
